# tip calculator
print("Welcome to the tip calculator.")
# we have to take input from user for our bill
bill=float(input("What was the total bill?\n"))

# now we will see how much per tip they have to give
tip=int(input("what percentage tip would you like to give? 10 12 15? \n"))

# and after this all calculation we split bill onto them
people=int(input("how many people to split the bill?\n"))

total_bill_tip=bill*(tip/100)
# we have to find percentage

total_bill=bill+total_bill_tip
# now we have to split bill into members

per_person=round(total_bill/people,2)
 
print(f"each person should pay:{per_person}")
